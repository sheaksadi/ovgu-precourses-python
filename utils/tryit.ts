/**
 * The online compiler the course sends students to. One place to change it:
 * the QR code, the link and the browser demo all read from here.
 *
 * Chosen because it runs Python 3 in the browser without an account.
 */
export const COMPILER = {
  /** What the QR code opens. */
  href: 'https://www.programiz.com/python-programming/online-compiler/',
  /** What the address bar and the link on the slide show. */
  display: 'programiz.com/python-programming/online-compiler',
  /** The breadcrumb line of the search result in the demo. */
  crumbs: 'programiz.com › python-programming › online-compiler',
  /** Result title in the demo. Generic on purpose: the demo copies no real design. */
  name: 'Online Python Compiler',
}
